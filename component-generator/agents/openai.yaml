interface:
  display_name: "Component Generator"
  short_description: "Korean UI component spec MD writer"
  default_prompt: "Use $component-generator to turn this Figma mockup or notes into a Korean UI component spec Markdown document."

policy:
  allow_implicit_invocation: true
